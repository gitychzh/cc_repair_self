# Copy this file to .env on each machine.
# POSTGRES_PASSWORD can differ per machine — it's a local secret.
# opc_uname uses: litellm_pg_2026
# opc2_uname uses: litellm_pg_pass
POSTGRES_PASSWORD=litellm_pg_pass
