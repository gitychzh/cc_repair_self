# R38.2: HM Proxy 三级 Fallback 路由重构

## 需求

1. NV 模型从4个减为3个：glm5.1 / deepseek-v4-pro / kimi-k2.6（去掉 minimax）
2. 默认 glm5.1 顺序路由 k1→k5，5个全 429 或空响应 → fallback kimi k1→k5 → fallback dsv4p k1→k5
3. 所有 fallback 从当前位置继续（不是从 k1 重新开始）
4. 日志详细记录 fallback 过程

## 改动清单

### 1. LiteLLM HM config (41101-41105) — 4dep→3dep

每个 config-k*.yaml 删除 `nvminimax_kN` deployment，保留 3 个：nvglm5.1 / nvkimi / nvdeepseek

### 2. config.py — 重构模型映射和 fallback 链

新增：
- `NV_MODEL_TIERS = ["glm5.1_hm", "kimi_hm", "deepseek_hm"]` — 按优先级
- `DEFAULT_NV_MODEL = "glm5.1_hm"` — 默认从 glm5.1 开始（不再是 kimi）

修改：
- `NV_MODEL_IDS` 去掉 minimax_hm
- `LITELLM_MODEL_MAP` 去掉 nvminimax
- `MODEL_MAP` 去掉 minimax 相关映射，默认 model→glm5.1_hm
- `MODEL_INPUT_TOKEN_SAFETY` 去掉 minimax_hm
- RR counter 改为 per-tier：`hm_nv_glm5.1` / `hm_nv_kimi` / `hm_nv_deepseek`
- `_next_hm_nv_key(tier_key)` 函数，per-tier 独立计数

### 3. upstream.py — 重构为三级 fallback

当前：固定1个模型循环5个key，5个全失败→all_keys_exhausted

改为三级 fallback：
```
execute_litellm_request(handler, oai_body, mapped_model, ...)
  → 确定 start_tier（mapped_model 在 NV_MODEL_TIERS 中的位置）
  → for tier in tiers[start_tier_idx:]:
      result = _try_tier_keys(handler, oai_body, tier, ...)
      if success and not empty_200: return result
      # tier 全失败 → HM-FALLBACK 日志 → 继续下一个 tier
  → 所有 tier 都失败 → all_keys_exhausted
```

`_try_tier_keys()`: 从当前 tier 的 RR counter 位置开始循环5个key
- 429/500/502 → cycling to next key
- 空200（choices=null/content=null/空choices）→ cycling to next key

**空200检测**：
- Non-streaming: 读取 resp body，检查 `choices` 是否 null/空/content 是否 null
- Streaming: Content-Length=0 判断（不读body避免破坏stream）

### 4. handlers.py — 更新

- mapped_model 用于确定 start_tier
- `/health`: 显示 `hm_model_tiers` + `hm_default_model`
- `/v1/models`: 只列3个模型 + aliases

### 5. docker-compose.yml — 无改动，但 LiteLLM 需 restart（config.yaml 变了）

### 6. CLAUDE.md + DEPLOY_STATUS.md — 更新

## Per-tier RR counter 设计

```python
def _next_hm_nv_key(tier_key: str) -> int:
    """Per-tier sequential RR: hm_nv_glm5.1 / hm_nv_kimi / hm_nv_deepseek"""
    with _vk_rr_lock:
        counter = _vk_rr_counter.get(tier_key, 0)
        key_idx = counter % HM_NUM_KEYS
        _vk_rr_counter[tier_key] = counter + 1
        _save_rr_counter()
        return key_idx
```

## 测试

1. `curl -s http://127.0.0.1:40006/health` — 确认 3 tier + glm5.1 default
2. glm5.1 正常请求 — 返回 nvglm5.1_kN
3. 请求 minimax — 应 fallback 到 glm5.1（不再有 minimax）
4. 检查日志确认 fallback 过程被记录
